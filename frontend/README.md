# Uni-drive

A university drive/storage web application.

Built with React 19 + Vite (JavaScript/JSX).

## Getting Started

```bash
cd frontend
npm install
npm run dev
```

## Commands

| Command | Purpose |
| --- | --- |
| `npm run dev` | Start the dev server |
| `npm run build` | Production build |
| `npm run lint` | Run ESLint |
| `npm run preview` | Preview the production build |

## Project Structure

```
frontend/
  src/
    pages/        Route-level pages (login, dashboard, drive browser)
    components/   Reusable UI pieces (buttons, modals, file cards)
    assets/       Images and static assets
  public/         Static assets served as-is
```

## For AI Agents

This repository contains an `AGENTS.md` at the root with rules that all AI agents must follow to keep the code clean and human-readable. Read it before making changes.
